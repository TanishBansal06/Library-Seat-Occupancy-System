# Server configuration
PORT=3000

# Oracle XE Database connection details
# If DB_PASSWORD is left blank or as the placeholder, the app runs in mock mode.
DB_USER=system
DB_PASSWORD=your_oracle_password_here
DB_CONNECTION_STRING=localhost:1521/XE
